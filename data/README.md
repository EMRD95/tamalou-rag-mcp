# Drop your sources here, then run:  tamalou-ingest
#
# Built-in loaders pick files up by extension:
#   *.pdf       → pdf loader        (collection: guide_pages)
#   *.md        → discord_md loader (collection: tamalou_memory)
#
# To add a new format, drop a file in src/tamalou_rag/loaders/.
